spring:
  application:
    name: meaasge-server

#logging 配置
logging:
  config: classpath:logback.xml
  level:
    org.springframework.web: debug
    com.dynamo.mail: debug

eureka:
  instance:
    ip-address: 192.168.1.136
    prefer-ip-address: true
